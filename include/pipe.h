#ifndef PIPE_H
#define PIPE_H

/**
 * @brief Ejecuta un comando con pipes.
 *
 * @param input El comando a ejecutar, que puede contener pipes.
 */
void execute_command_with_pipes(char* input);

#endif // PIPE_H